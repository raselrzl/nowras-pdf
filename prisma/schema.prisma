generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pdf {
  id        String   @id @default(cuid())
  name      String
  url       String
  createdAt DateTime @default(now())
}